<view 
  class="ls_check {{ baseClass }} {{ localChecked ? 'ls_check-checked' : '' }} {{ className }}" 
  style="{{ style }}" 
  onTap="onCheckTapHandler" 
  hidden="{{ hidden }}"
  data-nodeName="check"
></view>