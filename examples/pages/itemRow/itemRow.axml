<view class="page">
  <ls-check-group id="test" checkedList="{{ checkedList }}" onChange="onCheckedChangeHandler">
    <ls-item-row showCheck checkByContent checkGroupId="test" a:for="{{ source  }}" key="{{ item }}" source="{{ item }}">
      <view slot-scope="scoped">
        <text>库存: 900 {{ scoped.checked ? 'yes' : 'no'}}</text>
        <text>价格: 1900</text>
      </view>
    </ls-item-row>
  </ls-check-group>
</view>