<view 
  class="ls_check {{ baseClass }} {{ localChecked ? checkedType === 'check' ? 'ls_check-checked_check': 'ls_check-checked_circle' : '' }} {{ className }}" 
  style="{{ style }}" 
  onTap="onCheckTapHandler" 
  hidden="{{ hidden }}"
  data-nodeName="check"
></view>