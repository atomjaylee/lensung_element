<view
  class="ls_flex {{ baseClass }} {{ className }}"
  onTap="onTapHandler"
  onAppear="onAppearHandler"
  hidden="{{ hidden }}"
  style="{{ style }}"
  data-vip-code="{{ vipCode }}"
  data-statistic="{{ statistic }}"
>
  <slot></slot>
</view>